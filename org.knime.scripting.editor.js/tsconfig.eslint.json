// A simple tsconfig just such that eslint can make use of typescript features
{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": [ "src/**/*", "src/**/*.vue", "lib/**/*" ],
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node", "jsdom"]
  }
}
